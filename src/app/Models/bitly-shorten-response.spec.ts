import { BitlyShortenResponse } from './bitly-shorten-response';

describe('BitlyShortenResponse', () => {
  it('should create an instance', () => {
    expect(new BitlyShortenResponse()).toBeTruthy();
  });
});
