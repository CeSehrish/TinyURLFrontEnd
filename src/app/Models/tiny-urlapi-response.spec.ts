import { TinyURLapiResponse } from './tiny-urlapi-response';

describe('TinyURLapiResponse', () => {
  it('should create an instance', () => {
    expect(new TinyURLapiResponse()).toBeTruthy();
  });
});
